using UnityEngine;

namespace Assets.Scripts.Transformation
{
    public class TransformableObject : MonoBehaviour
    {
        public GameObject transformationPrefab;  // The prefab into which the player should transform.
        public bool scaleCorect;
    }
}